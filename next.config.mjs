/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["https://source.unsplash.com", "source.unsplash.com"],
  },
};

export default nextConfig;
